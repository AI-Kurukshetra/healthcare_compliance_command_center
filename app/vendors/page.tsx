import { RoutePlaceholder } from "@/components/ui/route-placeholder";

export default function VendorsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Vendors"
      title="Vendor risk route scaffolded."
      description="Add third-party review workflows, vendor inventories, and risk scoring here."
    />
  );
}
